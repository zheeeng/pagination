version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.8
    working_directory: /go/src/github.com/zheeeng/pagination
    steps:
      - checkout
      - run:
          name: Run unit tests
          command: go test ./...
